params    = { "something" }
arguments = {
    "(" ~ (params // bad comment
   ~ ("," ~ params)* // bad comment
   // comment dont know if this was a problem.
   ~ ","?)? ~ ")"
}
blah      = {
    ("w") ~ &("z")
}